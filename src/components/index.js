// Here we will import files from components as default and give them name, so we don't have to
// write exact path to each component and can just refere to the components folder 
import {default as Signin} from "../components/signin"
import {default as Sidebar} from "../components/sidebar"
import {default as Header} from "../components/header"